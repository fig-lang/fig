external js {
    fn log(input: i32);
    fn log_str(ptr: i32);
}

external env {
    fn sum(x: i32, y: i32): i32;
}

export fn main() {
    let x: i32 = "Hello World";
    let y: i32 = "Ok then";

    log_str(x);
    log_str(y);
}
