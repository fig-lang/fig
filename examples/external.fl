external console {
    fn log_str(ptr: i32);
}

external element {
    fn create(el: i32, text: i32);
}

export fn main() {
    let str: i32 = "Hello World";
    let sec_str: i32 = "Second String";

    str[0] = 1;
    sec_str[1] = sec_str[0] + 5;

    log_str(str);
    log_str(str[0]);
}
