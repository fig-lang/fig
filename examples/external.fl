external console {
    fn log_str(ptr: i32);
}

external element {
    fn create(el: i32, text: i32);
}

export fn main() {
    let str: i32 = "Hello World";
    let str2: i32 = "Second String";

    str[0] = 55;

    str2[1] = 33;

    log_str(str);

}
